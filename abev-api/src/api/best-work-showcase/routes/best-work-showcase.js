'use strict';

/**
 * best-work-showcase router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::best-work-showcase.best-work-showcase');
