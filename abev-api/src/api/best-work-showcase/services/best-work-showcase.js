'use strict';

/**
 * best-work-showcase service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::best-work-showcase.best-work-showcase');
